export type DrawGridStateValue = {};

const DrawGridState = (props) => {
  const $value: DrawGridStateValue = {};
  return {
    $value,

    selectors: {},

    actions: {}
  };
};

export default DrawGridState;
